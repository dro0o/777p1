import geomEach from "@turf/meta"
import intersect from "@turf/intersect"

export const regressionStuff = (hexNitrate, hexCancer) => {
  // Regression things
  console.log("Hex Nitrate: ", hexNitrate)
  console.log("Hex Cancer: ", hexCancer)

  var test = 0
  postMessage(test)
}
